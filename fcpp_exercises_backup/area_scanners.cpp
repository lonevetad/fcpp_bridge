
// the receiver nodes might use "bis_ksource_broadcast" providing, as value, the tuple < is_source, node.position(), node.uid, other_data? >
// this way, the sender nodes can send their data to the closest receiver
